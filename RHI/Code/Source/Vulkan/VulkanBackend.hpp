#pragma once

#include "RHI/RHI.hpp"

namespace rhi::vulkan {
    class DeviceManager final : public rhi::DeviceManager {
    public:
        DeviceManager()  = default;
        ~DeviceManager() = default;

    private:

    };
} // namespace rhi::vulkan
